{
  "name": "intentx-analytics-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    
    "prepare:intentx:base": "mustache ../configs/base_intentx_0.82.json template.yaml > subgraph.yaml; graph build",
    "deploy:intentx:base": "graph deploy --studio intentx-analytics_082",
    "prepare:intentx:blast": "mustache ../configs/blast_intentx_0.82.json template.yaml > subgraph.yaml; graph build",
    "deploy:intentx:blast": "graph deploy --studio intentx-analytics_082_blast",

    "prepare:test_bsc": "mustache ../configs/test_bsc_intentx_082.json template.yaml > subgraph.yaml; graph build",
    "deploy:test_bsc": "graph deploy --studio intentx_analytics_082_test_bsc",
    "prepare:core": "mustache ../configs/blast_core_0.82.json template.yaml > subgraph.yaml; graph build",
    "deploy:core": "graph deploy --studio core-analytics-082"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
