{
  "name": "result-quote",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "prepare:base": "mustache ../configs/base_intentx_0.82.json template.yaml > subgraph.yaml; graph build",
    "deploy:base": "graph deploy --studio intentx-main_082",
    "prepare:test_bsc": "mustache ../configs/test_bsc_intentx_082.json template.yaml > subgraph.yaml; graph build",
    "deploy:test_bsc": "graph deploy --studio intentx_main_082_test_bsc"
  }
}